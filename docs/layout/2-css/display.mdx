---
sidebar_position: 7
---

# 2.7 Свойство display и типы элементов

В этом уроке я расскажу тебе, как работает свойство display, какие бывают типы элементов (блочные, строчные и др.), и как это влияет на построение макета страницы.

## Что такое display

Свойство `display` определяет, как элемент будет вести себя на странице — будет ли он занимать всю строку, располагаться в одну линию с другими, или вообще не отображаться.

## Основные значения display

- `block` — блочный элемент (занимает всю ширину, начинается с новой строки)
- `inline` — строчный элемент (располагается в одну линию с другими, ширина по содержимому)
- `inline-block` — строчно-блочный (как строчный, но можно задавать ширину и высоту)
- `none` — элемент не отображается на странице

## Блочные и строчные элементы

- Блочные: `<div>`, `<p>`, `<h1>`, `<ul>`, `<li>`, `<section>`, `<header>` и др.
  - Блочные элементы всегда занимают всю ширину родителя, даже если содержимого мало. Они начинаются с новой строки и "выдавливают" следующие элементы вниз.
- Строчные: `<span>`, `<a>`, `<strong>`, `<em>`, `<img>` (по умолчанию), `<input>`, `<label>`
  - Строчные элементы располагаются в одну линию с другими, ширина и высота у них зависят только от содержимого. Для них нельзя задать width/height, а вертикальные margin и padding часто не работают как ожидается (используй горизонтальные).

### Пример

```html
<div>Я блочный</div>
<span>Я строчный</span>
<span>И я тоже</span>
```

Блочный элемент занимает всю ширину, а строчные идут друг за другом.

## Как изменить тип отображения

Иногда тебе нужно поменять поведение элемента:

```css
span {
  display: block;
}
div {
  display: inline;
}
```

Теперь `<span>` будет вести себя как блочный элемент, а `<div>` — как строчный.

## Строчно-блочные элементы (inline-block)

Иногда удобно, чтобы элемент располагался в одну линию с другими, но при этом можно было задать ему ширину и высоту. Для этого есть значение `inline-block` — оно сочетает свойства строчных и блочных элементов: можно управлять размерами и отступами, а элементы идут в одну строку.

```css
.button {
  display: inline-block;
  width: 120px;
  height: 40px;
  background: #2196f3;
  color: #fff;
  text-align: center;
  line-height: 40px;
  border-radius: 8px;
  margin: 8px;
}
```

## Скрытие элементов

Если ты хочешь временно скрыть элемент, используй `display: none;` — он исчезнет со страницы и не будет занимать место.

```css
.hidden {
  display: none;
}
```

Либо используй `visibility: hidden;` — элемент будет скрыт, но место он займёт.

```css
.hidden {
  visibility: hidden;
}
```

## Что использовать?

- Кнопки, карточки, секции — обычно блочные элементы (`block` или `inline-block`)
- Текстовые подписи, иконки, отдельные слова — строчные (`inline`)
- Если нужно, чтобы несколько элементов стояли в ряд, подумай: подойдёт ли `inline-block` или лучше сразу использовать flex/grid (это тема следующего урока)
- Помни: у блочных элементов вертикальные margin могут "схлопываться" (объединяться в один больший), это важно для аккуратной вёрстки

Понимание типов элементов и display поможет тебе:
- Избежать неожиданных переносов строк и "разъезжающихся" блоков
- Легко управлять размерами и выравниванием
- Быстрее и чище верстать макеты из Figma и других редакторов


## Практическое задание

Попробуй сверстать небольшой макет, чтобы потренироваться с разными значениями display:

1. Создай файл `display-homework.html` с такой структурой:

- Заголовок (например, "Мой первый макет")
- Подзаголовок (например, "Типы элементов и display")
- Три кнопки, которые должны стоять в одну линию
- Абзац, в котором внутри текста есть иконка (например, emoji)
- Скрытый блок с каким-нибудь текстом (он не должен быть виден на странице)

2. Создай файл `display-homework.css` и:
- Подбери для каждого элемента подходящее значение display, чтобы всё выглядело аккуратно
- Сделай так, чтобы кнопки были в одну строку, а абзац с иконкой выглядел красиво
- Спрячь скрытый блок с помощью display

<details>
<summary>Показать пример ответа</summary>

```html
<!DOCTYPE html>
<html>
<head>
  <title>Домашнее задание: display</title>
  <link rel="stylesheet" href="display-homework.css">
</head>
<body>
  <h1>Мой первый макет</h1>
  <h2>Типы элементов и display</h2>
  <div class="button-row">
    <button>Кнопка 1</button>
    <button>Кнопка 2</button>
    <button>Кнопка 3</button>
  </div>
  <p>Вот пример текста с иконкой <span class="icon">🌟</span> прямо внутри строки.</p>
  <div class="hidden-block">Этот блок не должен быть виден</div>
</body>
</html>
```

```css
.button-row {
  display: inline-block;
  /* Можно также использовать flex, но пока потренируйся с inline-block */
}
.button-row button {
  display: inline-block;
  margin-right: 8px;
}
.icon {
  display: inline;
  font-size: 1.2em;
}
.hidden-block {
  display: none;
}
h1, h2 {
  display: block;
  margin-bottom: 12px;
}
p {
  display: block;
  margin-bottom: 16px;
}
```

</details>

---

## Итоги

Теперь ты знаешь:
- Как работает свойство display
- Чем отличаются блочные, строчные и строчно-блочные элементы
- Как скрывать элементы с помощью display: none
- Как менять тип отображения элементов

В следующем уроке я расскажу тебе про Flexbox и выравнивание элементов на странице! 