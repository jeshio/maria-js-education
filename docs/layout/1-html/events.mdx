---
sidebar_position: 4
---

# 1.4 Атрибуты и события

## Введение

В этом уроке мы научимся делать наши страницы интерактивными. Ты узнаешь про атрибуты HTML-элементов и события, которые помогут добавить динамику на страницу.

---

## Атрибуты HTML

Атрибуты - это дополнительные свойства, которые можно задать HTML-элементам. С их помощью мы можем сделать элементы уникальными и добавить им специальное поведение.

### Основные атрибуты

1. **id** - уникальный идентификатор элемента. На странице не может быть двух элементов с одинаковым id:
```html
<div id="special-block">Особый блок</div>
```

2. **class** - это как группа или категория. В отличие от id, один и тот же класс можно использовать для многих элементов, как футболки одного цвета у команды:
```html
<div class="team-member">Игрок 1</div>
<div class="team-member">Игрок 2</div>
```

3. **title** - это подсказка, которая появляется, когда наводишь мышку на элемент:
```html
<span title="Тут спрятана подсказка!">Наведи на меня мышку</span>
```

---

## События JavaScript

События - это всё, что происходит на странице. Например, когда ты кликаешь мышкой, наводишь курсор на что-то или печатаешь. JavaScript помогает нам "поймать" эти действия и сделать что-то в ответ

### Основные события

1. **click** - событие клика по элементу. Есть два способа обработки:

**Старый способ:**
```html
<button onclick="handleClick()">Нажми меня</button>
```

**Современный способ:**
```html
<button id="myButton">Нажми меня</button>

<script>
  // Получаем элемент по id
  const button = document.getElementById('myButton');
  
  // Добавляем обработчик события click (первый аргумент - название события, второй - функция-обработчик),
  // то есть говорим кнопке: "Эй, когда по тебе кликнут, сделай вот это!"
  button.addEventListener('click', function() {
    // alert - это функция, которая показывает модальное окно с сообщением
    alert('Кнопка нажата');
  });

  // Можно добавить несколько обработчиков для одного и того же события
  button.addEventListener('click', function() {
    alert('Ещё один обработчик сработал');
  });
</script>
```

:::tip[Преимущества современного способа]
1. Разделение HTML и JavaScript
2. Возможность добавить несколько обработчиков одного и того же события для одного элемента
3. Лучший контроль над событиями
4. Соответствие современным стандартам
:::

2. **mouseover** и **mouseout** - события наведения курсора:

```html
<div id="hoverElement">Наведи курсор</div>

<script>
  const element = document.getElementById('hoverElement');
  
  element.addEventListener('mouseover', function() {
    console.log('Курсор наведён');
  });
  
  element.addEventListener('mouseout', function() {
    console.log('Курсор ушёл');
  });
</script>
```

### Объект события

Когда происходит любое событие (например, клик), JavaScript передаст в твой обработчик события объект с информацией об этом событии:

```html
<button id="myButton">Нажми меня</button>

<script>
  const button = document.getElementById('myButton');
  
  button.addEventListener('click', function(eventInfo) {
    // eventInfo - тот самый объект с информацией, можно назвать его как угодно,
    // обычно используют название event или e
    console.log('Элемент, по которому кликнули:', eventInfo.target);
    console.log('Тип события:', eventInfo.type);
    console.log('Где была мышка в момент клика:', eventInfo.clientX, eventInfo.clientY);
  });
</script>
```

:::tip[Как увидеть, что пишет console.log?]
`console.log`, как ты уже видела ранее, это функция, которая выводит информацию в консоль браузера. Чтобы увидеть её вывод:
1. Щёлкни правой кнопкой мыши где угодно на странице
2. Нажми "Просмотреть код" или "Инспектировать"
3. Найди вкладку "Консоль" - там будет появляться вся информация, которую выводит `console.log`
:::

---

## Практическое задание

1. Создай новый HTML файл:
```html
<!DOCTYPE html>
<html>
<head>
    <title>Интерактивная страница</title>
</head>
<body>
    <!-- Здесь будет код -->
</body>
</html>
```

2. Добавь:
   - Заголовок с текстом "Привет!"
   - Кнопку с текстом "Выйти из комнаты"
   - Параграф с текстом "Наведи курсор"

3. Добавь обработку событий:
   - При клике на кнопку текст заголовка должен меняться на "Пока!"
   - При наведении курсора на параграф нужно добавлять к его тексту "👋", при уходе курсора - возвращать исходный текст

<details>
    <summary>Подсказка</summary>

    ```html
<!DOCTYPE html>
<html>
<head>
    <title>Интерактивная страница</title>
</head>
<body>
    <h1 id="greeting">Привет!</h1>
    <button id="changeButton">Выйти из комнаты</button>
    <p id="hoverText">Наведи курсор</p>

    <script>
        const greeting = document.getElementById('greeting');
        const button = document.getElementById('changeButton');
        const text = document.getElementById('hoverText');

        button.addEventListener('click', function() {
            greeting.textContent = 'Пока!';
        });

        text.addEventListener('mouseover', function() {
            text.textContent = 'Наведи курсор 👋';
        });

        text.addEventListener('mouseout', function() {
            text.textContent = 'Наведи курсор';
        });
    </script>
</body>
</html>
    ```

</details>

---

## Итоги

В этом уроке ты научилась:
- Использовать атрибуты HTML-элементов
- Обрабатывать события на странице
- Применять современный подход к обработке событий
- Создавать интерактивные элементы

В следующем уроке мы познакомимся с HTML-формами! 